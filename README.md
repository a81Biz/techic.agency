# TECHIC — Where skill becomes art

SPA creada con **Vite + React + React Router (HashRouter)**.  
**Portabilidad total**: `base: './'` → el build funciona en cualquier dominio o subcarpeta (local, GH Pages, hosting propio).

## Requisitos
- Node 18+ (recomendado 20+)
- NPM

## Instalación
```bash
npm i
